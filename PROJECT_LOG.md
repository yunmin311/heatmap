# Project Log

> 项目变更日志（专业版）  
> 维护目标：记录每次改动的背景、范围、风险与回归结果，便于追溯。

---

## [2026-04-21] 文档完善 + 轻量缺陷修复

### 背景
- 需要在不大改结构的前提下，提高项目文档完整度；
- 需要补充一份更专业的项目日志；
- 对日志录入与展示流程做小范围稳定性修补。

### 变更范围
- 文档：
  - 重写 `README.md`，补齐项目说明、运行方式、数据模型、交互说明、已知情况；
  - 新增 `PROJECT_LOG.md`（本文件），作为后续持续记录载体。
- 代码（轻量）：
  - `src/App.tsx`
    - 统一新增日志时的 `createdAt` 时间戳来源，避免入库时间与前端状态时间不一致。
  - `src/components/Sidebar.tsx`
    - 链接类型判定改为大小写不敏感的 `http/https` 正则；
    - 新增保存前 `day` 格式基础校验（`yyyy-MM-dd`）；
    - 保存成功后同步清空链接输入框残留；
    - 标签渲染 key 改为 `tag-index`，避免重复标签导致 key 冲突。

### 影响评估
- 不涉及数据库 schema 变更；
- 不涉及 UI 结构重构；
- 不引入新依赖；
- 对现有功能影响面小，属于低风险修正。

### 验证结果
- `npm run build` ✅ 可通过；
- `npm run lint` ⚠️ 存在历史 lint 问题（集中在 `HeatmapScene.tsx`），本次未做大范围调整。

### 后续待办（建议）
- 将 `HeatmapScene.tsx` 的渲染纯度问题分支化处理；
- 增加日志输入校验失败提示（当前为静默 return）；
- 按版本持续记录 Project Log（建议每次 PR 更新一条）。
